Para correr la Tarea 4 se tiene que correr el siguiente comando en una terminal:

`mvn spring-boot:run`

Luego, en otra terminal se tiene que correr el comando:

`python -m flask_app.app`

Con esto la app debería estar funcionando.
